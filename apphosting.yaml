# Settings for Firebase App Hosting
# For details, see https://firebase.google.com/docs/app-hosting/configure

apiVersion: v1
runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 10
